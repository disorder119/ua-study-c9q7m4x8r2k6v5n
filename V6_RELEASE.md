# Alphabet Lab V6 release

V6 production output is bundled into `alphabet-core.bundle.js` and `alphabet-app.bundle.js`.

Final release validation passed on the V6 head:

- complete A1 / Legacy / V2 / V3 / V4 / V5 regression suite
- service-worker hardening regression
- adaptive-engine and persona simulations
- V5 production/mastery/migration/persona suites
- real Chromium desktop E2E
- real WebKit mobile E2E
- V6 schema persistence and reload

V6 keeps the existing learning behavior while consolidating production delivery, adding schema-6 aggregates, debounced persistence and a bundle-oriented service worker.
