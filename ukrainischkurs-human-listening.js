/* Ukrainischkurs für Joel · Human Listening Bridge v4
   Drei Tage echtes Audio-Diktat mit verifizierten menschlichen Aufnahmen.
   Ein Pflichtdurchgang besteht nur, wenn jede bewertete Tonspur wirklich Human-Audio war. */
(()=>{
  const VERSION=4,core=window.UKRAINIAN_LEARNING_CORE;if(!core)return;
  const start=D.length;
  const GROUPS=[['вода','аптека','магазин','автобус'],['лікар','тато','брат','туалет'],['будь ласка','Я не знаю','Німеччина','Україна']];
  const LESSONS=[['Hören: Alltag ohne Text','Nur hören und ukrainisch schreiben.','Vier bekannte Alltagswörter werden ausschließlich über menschliche Tonspuren abgefragt. Keine deutsche Auswahlantwort.',[]],['Hören: Personen und Orientierung','Klangbild statt Kartenbild.','Du musst vier weitere bekannte Wörter aus einer menschlichen Aufnahme selbst rekonstruieren.',[]],['Hören: Phrase und Herkunft','Auch mehrsilbige Formen sicher erkennen.','Zum Abschluss kommen zwei Phrasen und zwei Herkunftswörter. Erst echtes Human-Audio hören, dann ukrainisch tippen.',[]]];
  const TARGETS={};GROUPS.forEach((g,i)=>TARGETS[start+i]=g);LESSONS.forEach(x=>D.push(x));
  function ensure(){if(!s.humanListening||typeof s.humanListening!=='object')s.humanListening={version:VERSION,start,days:{}};s.humanListening.version=VERSION;s.humanListening.start=start;s.humanListening.days=s.humanListening.days||{};return s.humanListening}
  function required(){return Array.isArray(TARGETS[Number(s.day)])}
  function state(){const root=ensure(),k=String(s.day);return root.days[k]||(root.days[k]={passed:false,qualityPassed:false,best:0,humanBest:0,attempts:0,date:'',lastAudioQuality:''})}
  let session=null;function startSet(){window.UKRAINIAN_AUDIO_QUALITY?.resetEvidence?.();session={items:[...(TARGETS[Number(s.day)]||[])].sort(()=>Math.random()-.5),idx:0,correct:0,human:0,listened:false,source:'',misses:[]};renderBox()}
  function current(){return session?.items?.[session.idx]||''}
  function play(button){const text=current();if(!text)return;session.listened=false;session.source='loading';const label=document.querySelector('#humanListeningBox .hl-source');if(label)label.textContent='Audio wird geladen …';speak(text,button)}
  function sourceLabel(){if(!session)return '';if(session.source==='human')return '✓ Menschliche ukrainische Aufnahme';if(session.source==='tts-fallback'||session.source==='tts-unverified')return '⚠ Synthetische Hilfe – zählt nicht als Human-Nachweis';if(session.source==='loading')return 'Audio wird geladen …';return 'Noch nicht angehört'}
  window.addEventListener('ukrainian-audio-source',e=>{if(!session||core.normalize(e.detail?.text)!==core.normalize(current()))return;session.source=e.detail?.source||'';session.listened=true;renderBox()});
  window.addEventListener('ukrainian-audio-quality',e=>{if(!session||core.normalize(e.detail?.text)!==core.normalize(current()))return;if(e.detail?.source==='tts-unverified'){session.source='tts-unverified';session.listened=true;renderBox()}});
  function answer(value){if(!session?.listened){toast(session?.source==='loading'?'Audio wird noch geladen.':'Erst anhören, dann schreiben.');return}const wanted=current(),good=core.accepts(value,[wanted]);if(good){session.correct++;if(session.source==='human')session.human++}else session.misses.push(wanted);toast(good?'Richtig gehört.':'Gehört wurde: '+wanted);session.idx++;if(session.idx>=session.items.length){finish();return}session.listened=false;session.source='';renderBox()}
  function finish(){const st=state(),total=session.items.length,score=Math.round(session.correct/total*100),human=session.human,allCorrect=session.correct===total,allHuman=human===total,passed=allCorrect&&allHuman;st.best=Math.max(st.best||0,score);st.humanBest=Math.max(st.humanBest||0,human);st.attempts++;st.date=date();st.passed=passed;st.qualityPassed=passed;st.lastAudioQuality=allHuman?'human':'synthetic-or-unverified';core.recordSession({skills:['listening','writing'],correct:session.correct,total,passed,module:'human-listening',day:s.day,assisted:!allHuman,weight:allHuman?1.25:.45});session=null;if(passed)toast('Human-Audio-Diktat fehlerfrei bestanden: 4/4 mit menschlicher Quelle.');else if(allCorrect)toast('4/4 richtig, aber mindestens eine Tonspur war synthetisch. Das war Übung – für den Human-Nachweis erneut mit 4/4 menschlichen Aufnahmen.');else toast('Noch nicht stabil: für die Freigabe 4/4 mit menschlichen Aufnahmen in einem frischen Durchgang.');render()}
  function renderBox(){let box=document.getElementById('humanListeningBox');if(!required()){if(box)box.hidden=true;return}const cards=document.getElementById('cards');if(!cards)return;if(!box){box=document.createElement('section');box.id='humanListeningBox';box.className='card';cards.insertAdjacentElement('afterend',box)}box.hidden=false;const st=state();if(session){const pos=session.idx+1;box.innerHTML='<div class="hl-head"><div><div class="label">Human-Audio-Diktat · '+pos+' / '+session.items.length+'</div><h2>Nur hören. Dann Ukrainisch schreiben.</h2></div><div class="pill">'+session.correct+'/'+session.idx+'</div></div><p class="small">Kein Transkript und keine deutsche Auswahl. Nur menschliche ukrainische Aufnahmen können diesen Pflichtnachweis freischalten. Ein technischer TTS-Fallback bleibt reine Übung.</p><div class="actions"><button class="primary" id="hlListen">🔊 Aufnahme anhören</button></div><div class="hl-source">'+sourceLabel()+'</div><input id="hlInput" class="typing-input" lang="uk" autocapitalize="off" autocorrect="off" autocomplete="off" spellcheck="false" placeholder="Schreibe exakt, was du hörst …"><div class="actions"><button class="primary" id="hlCheck">Prüfen</button></div>';const input=document.getElementById('hlInput'),listen=document.getElementById('hlListen');listen.onclick=()=>play(listen);document.getElementById('hlCheck').onclick=()=>answer(input.value);input.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();answer(input.value)}};setTimeout(()=>input.focus(),0)}else box.innerHTML='<div class="hl-head"><div><div class="label">Hörtransfer · nur Human-Audio zählt</div><h2>Klang erkennen, nicht deutsche Hinweise erraten</h2></div><div class="pill">'+(st.passed?'✓ Human':'4/4')+'</div></div><p class="small">Vier bereits gelernte Wörter/Phrasen werden ohne sichtbaren Zieltext diktiert. Bestehen bedeutet ausdrücklich: vier richtige Antworten und vier menschliche Quellen.</p><div class="tip">'+(st.passed?'✓ Human-Hörnachweis bestanden. Bester Human-Durchgang: '+st.humanBest+'/4.':'Bester bisheriger Human-Anteil: '+(st.humanBest||0)+'/4. Synthetische Wiedergabe kann helfen, entsperrt den Kurs aber nicht.')+'</div><div class="actions"><button class="'+(st.passed?'secondary':'primary')+'" id="hlStart">'+(st.passed?'noch einmal':'Human-Diktat starten')+'</button></div>';const startBtn=document.getElementById('hlStart');if(startBtn)startBtn.onclick=startSet}
  const oldNext=document.getElementById('next')?.onclick;if(document.getElementById('next'))document.getElementById('next').onclick=function(e){if(required()&&!state().passed){renderBox();document.getElementById('humanListeningBox')?.scrollIntoView({behavior:'smooth',block:'center'});toast('Vor dem nächsten Tag erst 4/4 mit echten menschlichen Tonspuren bestehen.');return}return oldNext?.call(this,e)};
  const css=document.createElement('style');css.textContent='.hl-head{display:flex;gap:12px;justify-content:space-between;align-items:flex-start}.hl-source{text-align:center;font-size:.82rem;font-weight:800;margin:8px 0 12px;color:#526b87}';document.head.append(css);window.UKRAINIAN_HUMAN_LISTENING={version:VERSION,start,days:GROUPS.length,count:GROUPS.flat().length,humanOnlyPass:true,centralScoring:true};const previousRender=render;render=function(){previousRender();renderBox()};ensure();renderBox();
})();